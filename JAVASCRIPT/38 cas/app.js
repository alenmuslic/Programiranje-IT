//napraviti funkciju  koja brise duplikate iz niza i ignorisati case sensivity
let niz = ["Alen", "alen", "Amin", "amin"];

const second = (arr) => {
  const newArr = [];
  for (let i = 0; i < arr.length; i++) {
    if (arr.slice(0, i).includes(arr[i])) {
      continue;
    }
  }
};

let boje = ["Blue", "Green", "Red", "Orange", "Violet", "Indigo", "Yellw"];
o = ["st", "nd", "rd", "th"];

const first = (arr1) => {
  const brojac = [];
  for (let i = 0; i < arr.length; i++) {}
};
