const alen = ["alen", "muslic", "sadeta", "kurva"];

const istina = alen.filter((lol) => lol.length > 3);
console.log(istina);

const brojevi = [1, 3, 4, 6, 8, 2];
const veci = brojevi.map((x) => x + 2);
console.log(veci);
